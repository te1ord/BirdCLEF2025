hydra:
  run:
    # for augmentations runs
    dir: "../hydra_logs/augmentations"
  output_subdir: ${now:%Y-%m-%d_%H-%M-%S}

defaults:
  - _self_
  - data: data
  - augmentation: augmentation
  - model: model
  - training: training

# Global settings
seed: 42
debug: false
output_dir: outputs
experiment_name: logdirs/debug_run

device: "cuda"   # or "cpu" if you prefer

# Logging (if used later)
wandb:
  project: audio-classification
  entity: 'BashHav'
  name: ${experiment_name}
  tags: []
  
callbacks:
  n_checkpoints_to_save: 3
  metric_mode: max
  main_metric: valid_MultilabelAUROC
